# Network File 🖧



This is the configuration file used so that the system knows by default what to do.

```yaml
# Lobby's configuration of the startup -|- DreamNetwork™ 2022
type: DYNAMIC #Or STATIC ; You can change the type here  
xms: 1G # Minimal RAM
xmx: 2G # Maxmimal RAM
executable: paper_spigot_1_8_8 #the executable jar 
java-version: 8 #Java version, you can find your versions in the data folder in DreamNetwork in the JDKIndex.json
proxy: false
```

:::info

In the current version of DreamNetwork, you must restart it to apply the changes.

:::
